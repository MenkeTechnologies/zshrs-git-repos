[package]
name = "git_repos"
version = "0.1.1"
edition = "2021"
publish = false
description = "zsh-git-repo-cache ported to a native zshrs plugin (gitrepos)"

[lib]
name = "git_repos"
crate-type = ["cdylib", "rlib"]

[dependencies]
znative = "0.12"
